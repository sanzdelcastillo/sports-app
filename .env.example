# Copy this file to .env.local and paste your API-Football key.
# .env.local is git-ignored, so the key never ends up on GitHub.
# On Vercel, add the same name/value under Project → Settings → Environment Variables.
# API-Football (api-football.com) — fixtures, live, lineups, events, stats, tables. Required.
APIFOOTBALL_KEY=
